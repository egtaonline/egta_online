%tr
  %td= strategy
  %td= link_to 'Remove Strategy', url_for(:action => :remove_strategy, :role => role.name, :strategy_name => strategy), :method => :post, :id => "remove-#{role.name}-#{strategy}", :class => 'btn btn-mini btn-warning'