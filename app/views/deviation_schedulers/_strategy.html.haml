%tr
  %td= strategy
  %td= link_to 'Remove Strategy', url_for(:action => :remove_deviating_strategy, :role => role.name, :strategy => strategy), :id => "remove-dev-#{role.name}-#{strategy}", :method => :post, :class => 'btn btn-mini btn-warning'