%tr
  %td= strategy.name
  %td= link_to 'Remove Strategy', url_for(:action => :remove_deviating_strategy, :role => role.name, :strategy_name => strategy.name), :method => :post, :class => 'btn btn-mini btn-warning'