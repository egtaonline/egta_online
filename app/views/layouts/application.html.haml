!!!
%head
  %title Home - EGTA Online

  = stylesheet_link_tag 'compiled/screen.css', :media => 'screen, projection'
  = stylesheet_link_tag 'compiled/print.css', :media => 'print'
  /[if IE]
    = stylesheet_link_tag 'compiled/ie.css', :media => 'screen, projection'
  =javascript_include_tag :defaults
  =csrf_meta_tag

%body
  #root
    %header#top_header
      %h1#logo-text
        =link_to 'EGTA Online', '/'
      %nav#user_nav
        -if user_signed_in?
          = "Signed in as #{current_user.email} |"
          = link_to "Sign out", destroy_user_session_path
        -else
          #{link_to "sign in", new_user_session_path}
      %nav#object_nav
        %a#box{:href=>"/"}= "Home"
        %a#box{:href=>accounts_path}= "Accounts"
        %a#box{:href=>simulators_path}= "Simulators"
        %a#box{:href=>games_path}= "Games"
        %a#box{:href=>schedulers_path}= "Schedulers"
        %a#box{:href=>simulations_path}= "Simulations"
        %a#box{:href=>game_manipulations_path}= "Analysis"
    %section#main_section
      .notice= notice
      .alert= alert
      =yield
    #root_footer
  %footer#footer
    #bar
      #first_box
        %h3 Schedulers
        %p EGTA Online has #{pluralize @active_scheduler_count, "active scheduler"} out of #{pluralize @scheduler_count, "scheduler"}.
      #middle_box
        %h3 Simulations
        %p EGTA Online has #{pluralize @active_simulation_count, "active simulation"} and #{pluralize @complete_simulation_count, "complete simulation"}.
      #last_box
        %h3 Samples
        %p EGTA Online has #{pluralize @sample_count, "sample"} with #{pluralize @clean_sample_count, "clean sample"}.
    #copyright
      &nbsp; &copy;2010 Strategic Reasoning &nbsp; &nbsp;